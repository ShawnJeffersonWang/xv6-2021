1. a2
2. 编译器直接计算了 “f（8）+1=12” 的结果，并将其存储在 a1
3. 0000000000000630 <printf>
4. jalr rd, offset(rs1) 将存储 pc+4 in rd 的值。如果 rd 是 ra ，则该指令实质上成为函数调用
5. 输出为： He110 World 。如果 RISC-V 是 big-endian，则设置 i 和 dlr 将产生相同的输出
6.  y 打印值未定义。在调用 printf 之前，编译器会将输入参数放入寄存器中 a0-a7 。当跳转到 printf 时，将从寄存器中读取值。但是，由于我们没有为第二个占位符指定值， a2 因此没有修改。因此，print out 的值 y 是在函数调用 a2 之前存在的任意值